# Agents

## Golang Standards

When creating golang code do so in a way that is compiant with `gopls` LSP.
Things like requiring package level comment is a common miss, ensure you don't
miss doing this.
